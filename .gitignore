# Build outputs
build/bin
frontend/dist

# Wails-generated build assets (regenerated from embedded templates each build).
# The surrounding build/<platform>/ files ARE tracked on purpose: they carry the
# bundle identifier, entitlements and product metadata the release workflow needs.
build/windows/installer/wails_tools.nsh
build/windows/icon.ico

# Node
node_modules

# Binaries / packages
*.app
*.exe
*.deb
*.rpm

# OS
.DS_Store

# Project-local binary artifact
/intraflow

# Tooling / planning (not part of the shipped app)
openspec/
.opencode/